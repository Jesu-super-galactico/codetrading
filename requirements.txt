
# Dependencias mínimas para ejecutar la aplicación
fastapi
# ASGI server (usa uvicorn en desarrollo; en producción puedes usar gunicorn+uvicorn workers)
uvicorn
gunicorn

# Procesamiento y captura de vídeo
opencv-python
numpy

# Descarga/extraer URLs de streaming (yt-dlp es un fork moderno de youtube-dl)
yt-dlp

# Visualización opcional (si usas la parte con ventana Pygame)
pygame

# Nota: añade versiones si quieres pines específicos, e.g. opencv-python==4.7.0.72
